# Settings to manage and configure a Firebase App Hosting backend.
# https://firebase.google.com/docs/app-hosting/configure

runConfig:
  cpu: 1
  memoryMiB: 1024
  concurrency: 80
  maxInstances: 10
  
env:
  - variable: NEXT_PUBLIC_REACTIVE_VERSION
    value: "1.0.0-zero-frame"
